{{

  "$schema": "https://openapi.vercel.sh/vercel.json",  "rewrites": [

  "buildCommand": "cd 'Tampa APP' && npm ci --legacy-peer-deps && npm run build",    {

  "outputDirectory": "Tampa APP/dist",      "source": "/(.*)",

  "installCommand": "echo 'Skipping root install'",      "destination": "/index.html"

  "rewrites": [    }

    {  ]

      "source": "/(.*)",}
      "destination": "/index.html"
    }
  ]
}
